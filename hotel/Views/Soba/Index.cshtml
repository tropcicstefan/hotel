@model IEnumerable<hotel.Models.Soba>

@{
    ViewBag.Title = "Index";
}

<h2>Sobe</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Broj sobe
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Hotel.Naziv) hotela
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SobaTip.Naziv) tipa sobe
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Hotel.Naziv)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SobaTip.Naziv)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.ID }) |
            @Html.ActionLink("Details", "Details", new { id = item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.ID })
        </td>
    </tr>
}

</table>
